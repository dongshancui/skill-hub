# CLAUDE.md

## 操作原则

1. **每次改动完自动推送 git**：文件新增或修改完成后，执行 `git add . && git commit -m "<简短描述>" && git push`，不需要询问确认。

2. **文档用中文写**：所有 `.md` 文件内容用中文，代码块、命令、工具名保持英文原样。

3. **只写文件，不做口头总结**：结论和内容写进文件，不在对话里重复输出文档内容。

4. **不创建不必要的文件**：每个主题一个文件，不拆分，不新建重复内容的文件。
